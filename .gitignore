# ─── SpendTrack .gitignore ───────────────────────────────────

# Config file containing API keys — NEVER commit this.
# For local dev: edit config.js directly.
# For Vercel:    build.js auto-generates it from env variables.
config.js

# Node (only needed if you add more build tooling)
node_modules/

# OS clutter
.DS_Store
Thumbs.db

# Editor folders
.vscode/
.idea/
